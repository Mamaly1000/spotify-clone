/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "vdjiokoticvfsmlbelte.supabase.co",
      },
    ],
  },
};

module.exports = nextConfig;
