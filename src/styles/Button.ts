import CustomButton from "@/components/inputs/Button";
import { styled } from "@mui/material";

export const StyledFab = styled(CustomButton)({
  position: "absolute",
  zIndex: 1,
  top: -30,
  left: 0,
  right: 0,
  margin: "0 auto",
});
